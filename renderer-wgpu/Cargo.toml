[package]
name = "renderer-wgpu"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib", "staticlib"]

[features]
default = []
renderdoc = ["dep:renderdoc"]

[dependencies]
bytemuck = "1.25.0"
pollster = "0.4.0"
renderdoc = { version = "0.12.1", optional = true }
wgpu = "28.0.0"
